S3 Bucket accessing from Private Subnet using 'Endpoint' VPC service :-
In VPC:-
           Enabling Host name
Security Group:-
                  Inbound rule allowing allowing public access

Using AWS CLI to manage S3-Bucket and Contents of Bucket:- 
Commands are:- (make bucket) mb{}
               (remove bucket) rb{}
               (copy file) cp{}
               (remove file) rm{}
